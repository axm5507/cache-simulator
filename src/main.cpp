#include "Cache.h"
#include "TraceReader.h"
#include "Config.h"
#include <iostream>

int main(int /*argc*/, char* /*argv*/[]) {
    std::cout << "Cache Simulator — skeleton build OK\n";
    return 0;
}
